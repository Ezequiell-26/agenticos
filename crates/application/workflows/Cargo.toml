[package]
name = "agenticos-workflows"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
agenticos-sqlite-migrations = { path = "../../utilities/sqlite-migrations" }
agenticos-contracts = { path = "../../domain/contracts" }
sqlx = { workspace = true, features = ["sqlite", "runtime-tokio-rustls"] }
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
tokio = { workspace = true, features = ["sync", "rt-multi-thread", "macros"] }

[lints]
workspace = true
