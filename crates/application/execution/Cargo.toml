[package]
name = "agenticos-execution"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
agenticos-contracts = { path = "../../domain/contracts" }
agenticos-kernel = { path = "../../infrastructure/kernel" }
agenticos-memory = { path = "../../infrastructure/memory" }
async-trait = "0.1"
tokio = { version = "1.0", features = ["sync", "rt-multi-thread", "macros"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
tokio = { version = "1.0", features = ["sync", "rt-multi-thread", "macros"] }

[lints]
workspace = true
