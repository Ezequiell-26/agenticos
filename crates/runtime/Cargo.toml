[package]
name = "agenticos-runtime"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
agenticos-contracts = { path = "../contracts" }
agenticos-kernel = { path = "../kernel" }

[lints]
workspace = true
